[
  {
    "title": "GatedSibling does NOT auto-receive GatedService's session — the sibling-borrow was removed; a project-wide session is now explicit via `probe --save-session -g` (see CLI.probe)",
    "namespace": { "serviceId": "GatedSibling", "moduleName": "Auth", "methodName": "getSession" },
    "Before": [
      {
        "title": "Sign in on GatedService (:5556) — captures a cookie for THAT origin only",
        "namespace": { "serviceId": "GatedService", "moduleName": "Auth", "methodName": "signIn" },
        "args": [
          {
            "name": "argument:",
            "input": { "email": "test@test.com", "password": "abc123" },
            "input_type": "object",
            "data_type": "",
            "targetValues": []
          }
        ],
        "savedEvaluations": [
          {
            "namespace": "results.success",
            "expected_type": "boolean",
            "validations": [{ "name": "boolEquals", "value": true }],
            "save": true
          }
        ]
      }
    ],
    "Main": [
      {
        "title": "getSession on this sibling (:5557) sees NO session — nothing auto-rode from GatedService (borrow gone)",
        "namespace": { "serviceId": "GatedSibling", "moduleName": "Auth", "methodName": "getSession" },
        "args": [],
        "savedEvaluations": [
          {
            "namespace": "results.cookie",
            "expected_type": "string",
            "validations": [{ "name": "strEquals", "value": "" }],
            "save": true
          }
        ]
      }
    ],
    "Events": [],
    "After": []
  }
]
