# Idea: The AI-driven doc window → folded into RFC-018

> This idea graduated. It's been merged whole into **[RFC-018: The AI Window](../../RFCs/RFC-018-ai-report.md)** —
> along with the "AI writes saved communications the UI renders" framing and the CodeMirror editor.
>
> The RFC keeps everything this file had (the stage / "AI drives, UI vouches" split, multi-pane layouts,
> the plugin file/code providers, `assemble` "show me all the parts", the reverse channel, CodeMirror) and
> adds: **two lifetimes** (live stage + saved views on `.systemview/` namespaces), the **`diff`** and
> **`test`** blocks, and phased rollout.
>
> Go to RFC-018.
